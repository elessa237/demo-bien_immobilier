{{ form_start(form) }}
<div class="row form-group">
    <div class="col-8">
        <div class="row mb-3">
             {{ form_row(form.nom) }}
        </div>
        <div class="row mb-3">
            <div>
                <button class="btn btn-success">{{ button_label|default('Enregistrer') }}</button>
            </div>
        </div>
    </div>
</div>
{{ form_end(form) }}